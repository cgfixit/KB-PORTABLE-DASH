## Summary

<!-- What changed and why. -->

## Checks

- [ ] `ruff check .` and `pytest` pass locally
- [ ] No live passwords, API keys, or customer rows in the diff
- [ ] `config.toml` and `data/` stay gitignored
